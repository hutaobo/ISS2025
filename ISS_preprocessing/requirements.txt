python=3.10
numpy<2.0           # 保持在 1.x 系列，避免与 scikit-image 等二进制不兼容
scipy>=1.10,<1.11   # 与 NumPy 1.x 匹配
pandas>=2.0,<2.1    # 兼容 NumPy 1.x
scikit-image<0.20   # 与 NumPy 1.x ABI 兼容
ashlar              # 回退到无缺少 quiet 参数的版本
opencv-python       # cv2 接口
tifffile            # TIFF 读写
aicspylibczi        # .czi 读取
mat73               # .mat 读取
readlif             # .lif 读取
tqdm                # 进度条
requests            # HTTP 请求
